<?php
namespace OAuth2Provider\Containers;

class ConfigContainer extends AbstractContainer
{
    public function getServerContentsFromKey($server, $key)
    {
        // not supported
    }

    public function isExistingServerContentInKey($server, $key)
    {
        // not supported
    }
}
